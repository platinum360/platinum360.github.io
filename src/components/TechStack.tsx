import DomeGallery from "./DomeGallery";

const TechStack = () => {
  return (
    <div className="techstack" id="gallery">
      <h2 className="light-green-heading">My <span style={{ color: "var(--textColor)" }}>Playground</span></h2>
      <DomeGallery />
    </div>
  );
};

export default TechStack;
